id: standardfeil-til-andel
term_no: Standardfeil til utvalgsandelen (SE(pˆ))
short_def: "`SE(pˆ) = √(pˆ(1 − pˆ)/n)` — spredningen i estimert andel `pˆ = X/n`."
filters:
  random_variable: [proportion]
  parameters_known: [success_p, confidence_level]
  computes: [std_dev, confidence_interval, hypothesis_test]
  distribution_assumption: [normal, binomial]
  setup: [single_population]
  structural_cues: [success_probability_given]
  tooling: [standard_normal_table_E3]
long_def: |
  Standardfeilen til andelen brukes i z-test og KI for andel. To
  varianter er vanlige:

  **For KI** (bruker estimat):

      SE(pˆ) = √( pˆ(1 − pˆ) / n )

  **For testobservator** (bruker `p₀` fra `H₀`):

      SE(pˆ) = √( p₀(1 − p₀) / n )

  Forutsetter normalapproksimasjon:

      n · pˆ · (1 − pˆ) ≥ 5

  **Trenger rådata?** Nei — bare `n` og `pˆ` (eller `p₀`).

  Standardfeilen er størst når `pˆ = 0.5` og minst når `pˆ` er nær `0` eller `1`.
aliases:
  - standardfeil til andel
  - standardfeil til andelen
  - se(pˆ)
  - se(p)
see_also:
  - { id: andel, kind: glossary }
  - { id: en-utvalg-z-test-andel, kind: entry }
  - { id: ki-andel-binomial, kind: entry }
  - { id: normalapproksimasjon, kind: glossary }
