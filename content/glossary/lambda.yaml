id: lambda
term_no: Lambda (λ)
short_def: "Rate — antall hendelser per tids- eller romenhet i poisson- og eksponentialfordelingen."
filters:
  parameters_known: [rate_lambda]
  setup: [events_in_window]
  structural_cues: [rate_given, process_over_time, counting_events, poisson_process_mentioned, time_until_first]
  distribution_assumption: [poisson, exponential]
  random_variable: [time_until_event]
aliases:
  - λ
  - lambda
long_def: |
  `λ` er rateparameteren.

  **Poisson** — sannsynlighet for `k` hendelser:

      P(X = k) = e^(−λ) · λ^k / k!

  der `λ` er forventet antall hendelser i intervallet.

  **Eksponential** — tid mellom hendelser med rate `λ`:

      E[T] = 1/λ

  Pass på enheter — hvis raten er per minutt, må `t` også være i minutter.
  Når oppgaven gir «rate» eller «gjennomsnittlig antall», er det `λ`.
see_also:
  - { id: poisson-fordeling, kind: entry }
  - { id: eksponential-fordeling, kind: entry }
  - { id: poisson-prosess, kind: glossary }
