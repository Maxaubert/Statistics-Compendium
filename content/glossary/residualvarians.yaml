id: residualvarians
term_no: Residualvarians (S_E²)
short_def: "Variansen til residualene i regresjon: `S_E² = Σ(yᵢ − ŷᵢ)² / (n − 2)`."
filters:
  setup: [linear_relationship]
  parameters_known: [residual_variance, sum_xx_sxy, slope_se]
  computes: [std_dev]
  random_variable: [slope_estimate]
  structural_cues: [linear_relationship_suspected, data_as_list]
long_def: |
  Formel:

      S_E² = Σ(yᵢ − ŷᵢ)² / (n − 2)

  Måler hvor langt observasjonene ligger unna regresjonslinjen i snitt.
  Brukes som mellomsteg i nesten alt videre i lineær regresjon.

  **Trenger rådata?** Indirekte: trenger `Σ(yᵢ − ŷᵢ)²` (som regnes fra
  `(xᵢ, yᵢ)`-parene). Hvis `S_E²` er oppgitt direkte, trenger du ikke
  rådataene.

  **Hvorfor `n − 2`?** To frihetsgrader er «brukt opp» til å estimere
  `αˆ` og `βˆ` fra de samme dataene.

  Inngår i: standardfeil til `βˆ`, hypotesetest for stigningstall,
  prediksjonsintervall.
aliases:
  - residualvarians
  - residualvariansen
  - s_e²
  - se²
see_also:
  - { id: regresjon-residualvarians, kind: entry }
  - { id: residualstandardavvik, kind: glossary }
  - { id: residual, kind: glossary }
  - { id: varians-oversikt, kind: entry }
